# ToDo アプリ システム仕様書

## 概要

このシステムは、Python の Flask フレームワークを使用して構築されたシンプルな ToDo 管理ウェブアプリケーションです。Devin での機能追加学習を目的として、最小構成で設計されています。

## システム構成

### 技術スタック
- **バックエンド**: Python 3.11 + Flask
- **フロントエンド**: HTML + CSS（インライン）
- **データストレージ**: SQLite データベース（tasks.db）
- **コンテナ化**: Docker + Docker Compose

### ファイル構成
```
devin_sample_todo_app/
├── app.py              # メインアプリケーション
├── tasks.db            # SQLiteデータベースファイル
├── requirements.txt    # Python依存関係
├── Dockerfile          # Dockerイメージ定義
├── docker-compose.yml  # Docker Compose設定
├── templates/
│   └── index.html      # UIテンプレート
└── README.md           # プロジェクト説明
```

## 機能仕様

### 基本機能

#### 1. タスク表示機能
- **エンドポイント**: `GET /`
- **機能**: 保存されているすべてのタスクを表示
- **実装**: `app.py:19-21`
- **データ取得**: `load_tasks()` 関数でSQLiteデータベースから読み込み

#### 2. タスク追加機能
- **エンドポイント**: `POST /add`
- **機能**: 新しいタスクをリストに追加
- **実装**: `app.py:63-69`
- **パラメータ**: `title` (必須), `due_date` (オプション)
- **データ保存**: `add_task()` 関数でSQLiteデータベースに保存

#### 3. タスク削除機能
- **エンドポイント**: `POST /delete/<task_id>`
- **機能**: 指定されたIDのタスクを削除
- **実装**: `app.py:71-74`
- **パラメータ**: `task_id` (URL パラメータ、必須)
- **データ削除**: `delete_task()` 関数でSQLiteデータベースから削除

#### 4. 締切日管理機能
- **機能**: タスクに締切日を設定し、状態に応じて表示を変更
- **実装**: `app.py:44-61` (インデックス表示), `app.py:63-69` (追加処理)
- **締切状態**: 期限切れ（赤色）、今日が期限（オレンジ色）、未来の期限（通常色）
- **ソート**: 締切日順にタスクを自動ソート（期限なしタスクは最後）

### データモデル

#### タスクオブジェクト
```json
{
  "id": "string",       // タスクの一意識別子（UUID、必須）
  "title": "string",    // タスクのタイトル（必須）
  "due_date": "string", // 締切日（YYYY-MM-DD形式、オプション）
  "status": "string"    // 締切状態（overdue/due-today/upcoming、表示時のみ）
}
```

#### データファイル構造
```json
[
  {"id": "8d4a3298-0493-46a8-9437-c59e5592328c", "title": "期限切れタスク", "due_date": "2025-07-20"},
  {"id": "f2b1c4e5-8a7d-4c3b-9e2f-1a8b7c6d5e4f", "title": "今日が期限", "due_date": "2025-07-24"},
  {"id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890", "title": "期限なしタスク"},
  ...
]
```

## インターフェース仕様

### Web UI
- **URL**: `http://localhost:5000/`
- **レイアウト**: シンプルな単一ページ構成
- **要素**:
  - ページタイトル: "My ToDo List"
  - タスク入力フォーム（テキストフィールド + 締切日フィールド + 追加ボタン）
  - タスクリスト（箇条書き表示、締切日付きで状態別色分け）

### API仕様

#### GET /
- **用途**: メインページの表示
- **レスポンス**: HTMLページ（タスクリスト込み）

#### POST /add
- **用途**: 新規タスクの追加
- **リクエストボディ**: `application/x-www-form-urlencoded`
  - `title`: タスクタイトル（required）
  - `due_date`: 締切日（YYYY-MM-DD形式、optional）
- **レスポンス**: リダイレクト（`/` へ）

#### POST /delete/<task_id>
- **用途**: 指定されたタスクの削除
- **URLパラメータ**: 
  - `task_id`: 削除するタスクのID（required）
- **レスポンス**: リダイレクト（`/` へ）

## データ管理

### ストレージ
- **形式**: SQLite データベース
- **場所**: `tasks.db`（アプリケーションルート）
- **テーブル構造**: 
  ```sql
  CREATE TABLE tasks (
      id TEXT PRIMARY KEY,
      title TEXT NOT NULL,
      due_date TEXT
  );
  ```

### データ操作関数
- `init_db()`: データベースとテーブルの初期化
- `load_tasks()`: SQLiteデータベースからタスクデータを読み込み
- `add_task(title, due_date)`: 新しいタスクをデータベースに追加
- `delete_task(task_id)`: 指定されたタスクをデータベースから削除

## デプロイメント

### ローカル実行
```bash
python app.py
```
- ポート: 5000
- アクセス: `http://localhost:5000`

### Docker実行
```bash
docker-compose up
```
- ポート: 5000（ホスト側も5000にマッピング）
- ボリュームマウント: カレントディレクトリを `/app` にマウント
- 再起動ポリシー: `unless-stopped`

## 制限事項・課題

### 現在の制限
1. **タスク編集機能なし**: 一度追加したタスクの変更不可
2. **タスク完了状態なし**: 完了/未完了の管理機能なし
3. **高度なデータベース機能未使用**: 基本的なSQLite操作のみ
4. **認証機能なし**: 誰でもアクセス・操作可能
5. **エラーハンドリング最小**: 基本的な入力チェックのみ

### 拡張可能性
- タスク編集機能の追加
- タスク完了状態の管理
- 締切日通知機能
- タスクの優先度設定
- カテゴリ・タグ機能
- より高度なデータベース機能（インデックス、トランザクション等）
- ユーザー認証機能
- RESTful API の提供

## セキュリティ考慮事項

### 現在の状況
- 入力値のサニタイゼーション: Flask のテンプレートエンジンによる基本的なエスケープ
- CSRF 対策: 未実装
- 認証・認可: 未実装

### 推奨改善点
- CSRF トークンの実装
- 入力値検証の強化
- セッション管理の追加
- HTTPS 対応

## 運用・保守

### ログ
- 現在のログ出力: Flask の標準ログのみ
- アクセスログ: 未実装

### バックアップ
- データベースファイル（`tasks.db`）の定期バックアップ推奨

### 監視
- アプリケーションの稼働状態確認
- ディスク容量の監視（ログファイル増大対策）